{
    "name": "node-mappedbuffer",
    "main": "./lib/mappedbuffer",
    "version": "0.0.1",
    "description": "Buffer to store the memory that is mapped by the mmap",
    "tags": ["buffer"],
    "keywords": ["buffer", "mmap"],
    "author": "kazupon (kazuya kawaguchi) <kawakazu80@gmail.com>",
    "repository": {
        "type": "git",
        "url": "https://github.com/kazupon/node-mappedbuffer.git"
    },
    "bugs": {
        "url": "https://github.com/kazupon/node-mappedbuffer/issues"
    },
    "dependencies": {
        "bindings": "1.0.0"
    },
    "devDependencies": {
        "mocha": "*",
        "should": "*"
    },
    "engines": {
        "node": ">= 0.8.12"
    }
}
